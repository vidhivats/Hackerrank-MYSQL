select case
            when a+b>c then
            case
            when a=b and b=c then "Equilateral"
            when a=b or b=c or c=a then "Isosceles"
            when a!=b and b!=c then "Scalene"
            end
            else "Not A Triangle"
            end 
            from triangles;